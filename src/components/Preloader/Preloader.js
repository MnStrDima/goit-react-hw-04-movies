import Loader from 'react-loader-spinner';

const Preloader = () => (
  <Loader
    className="loader"
    type="ThreeDots"
    color="#ffb6c1"
    height={100}
    width={100}
  />
);
export default Preloader;
